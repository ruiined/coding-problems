import assert from "assert/strict";

/* 



*/

const func = () => {
  return;
};

/*
  Time complexity: 
  Space complexity: 
*/

assert.deepEqual(func(), "");
assert.deepEqual(func(), "");
assert.deepEqual(func(), "");
