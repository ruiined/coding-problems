import { strict as assert } from "assert";

/* 



*/

const func = () => {
  return;
};

/*
  Time complexity: 
  Space complexity: 
*/

assert.deepEqual(func(), "");
assert.deepEqual(func(), "");
assert.deepEqual(func(), "");
